id: no-automatic-fallback
language: TypeScript
severity: error
message: Use explicit provider selection; do not implement automatic fallback chains.
rule:
  pattern: |
    try {
      await $PRIMARY.$PRIMARY_METHOD($$$PRIMARY_ARGS);
    } catch ($ERR) {
      await $SECONDARY.$SECONDARY_METHOD($$$SECONDARY_ARGS);
    }
